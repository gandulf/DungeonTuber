This is a python project that uses PyQT6 and native vlc

The main script is in DungeonTuber.py which uses the modules components, config and logic with the apropriate classes.
Additional assets are stored under assets and docs. For translations the locales directory with the gettext library are used.